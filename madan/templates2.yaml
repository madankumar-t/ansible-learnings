---
   - hosts: localhost
     become_method: yes
     gather_facts: no

     tasks: 
     - template:
         src: /tmp/hosts.tmp
         dest: /etc/hosts
       register: output
     - debug: msg= "{{ output }}"
